package funkin.backend;

enum abstract InputType(String) from String to String {
    var HOLD:String = 'hold';
    var PRESS:String = 'press';
    var RELEASE:String = 'release';
}

class Controls {
    
}